function add(left,right){
    var result = left + right;
        document.write(left+'+'+right+'=');
        return left+right
}
function sub(left,right){
    var result = left - right;
        document.write(left+'-'+right+'=');
        return left-right
}

function mul(left,right){
    var result = left * right;
        document.write(left+'x'+right+'=');
        return left*right
}

function div(left,right){
    var result = left / right;
        document.write(left+'/'+right+'=');
        return left/right
}
